CREATE TABLE IF NOT EXISTS users(
    id INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(50) NOT NULL,
    email VARCHAR(50) NOT NULL UNIQUE,
    password VARCHAR(70) NOT NULL,
    role ENUM('admin','user') NOT NULL DEFAULT 'user'
);

CREATE TABLE IF NOT EXISTS employees(
    id INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(50) NOT NULL,
    nrc VARCHAR(50) NOT NULL UNIQUE,
    education VARCHAR(50) NOT NULL,
    phone INT NOT NULL UNIQUE,
    dob DATE NOT NULL,
    dept VARCHAR(15) NOT NULL,
    position VARCHAR(15) NOT NULL,
    status ENUM('0','1') NOT NULL,
    image MEDIUMTEXT,
    other_qulification TEXT
);

dummy data;

INSERT INTO employees(name,nrc,education,phone,dob,dept,position,status)
VALUES('Thaw Thaw','14/PaTaNa(N)0234568','B.E',09123456789,'2019-10-4','M&E','Chief Engineer','0');